
import {
    Dimensions,
} from 'react-native';

const {height, width} = Dimensions.get('window');

const CONSTANTS = {
    SCREEN_HEIGHT: height,
    SCREEN_WIDTH: width,
  };
  
export default CONSTANTS;